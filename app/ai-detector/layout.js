const AIDetectorLayout = ({ children }) => {
  return (
    <div className="h-full w-full bg-gradient-to-br from-blue-100 via-indigo-100 to-purple-100 font-monaspace_xenon">
      {children}
    </div>
  );
};

export default AIDetectorLayout;
